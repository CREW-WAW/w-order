server:
  port: 8083

eureka:
  instance:
    instance-id: product-microservice-instance
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8888/eureka

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/w-order?useUnicode=true&characterEncoding=utf8&useSSL=false
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: root
    password: '!!kdk236'
  jpa:
    database: mysql
    generate-ddl: true
    show-sql: true

logging.level:
  org.hibernate.SQL: debug
